module.exports = {
      content: [
        './index.html',
        './src/**/*.{html,js,ts,jsx,tsx}', // Adjust based on your project structure
        './public/**/*.html', // Include HTML files in a 'public' directory
        // Add more paths as needed for nested files
      ],
      theme: {
        extend: {},
      },
      plugins: [],
    };